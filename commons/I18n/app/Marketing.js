import { defineMessages } from 'react-intl'

const ns = 'app.Marketing'
const m = defineMessages({
  title: {
    id: `${ns}.header.title`,
    defaultMessage: 'With Ephemeral Chat, Encrypted Voice and Video Calling, and End-to-End Encrypted Email.'
  }
})

export default m
